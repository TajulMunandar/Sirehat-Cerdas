export type TUser = {
    id?: number;
    username: string;
    role: number;
    error: any
};

export type TAUser = {
    id?: number;
    username: string;
    password: string;
    role: number;
};
